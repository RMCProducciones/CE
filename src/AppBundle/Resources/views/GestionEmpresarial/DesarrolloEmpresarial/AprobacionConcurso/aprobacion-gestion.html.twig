{% extends 'AppBundle::frontend.html.twig' %}


{% block title %}Gestion Informacion Financiera {% endblock %}

{% block breadcrumb_ul %}

    <ul class="breadcrumb">
      <li><a href="{{ path('homepage') }}">Inicio</a></li>
      <li>Gestión Empresarial</li>
      <li><a href="{{ path('concursoGestion') }}">Concurso</a></li>
      <li class="active">Aprobación Concurso</li>
    </ul>
{% endblock %}





{% block section %}
  <div class="container">
    <h1>Aprobación de Concurso</h1>
    <hr>
  </div>

  <br />
  <div class="section">
      <div class="container">
        <div class="row">
          <div class="col-md-6">
            <div class="btn-group">
              <a href="{{ path('concursoGestion', {'idConcurso': idConcurso}) }}" class="btn btn-warning">Volver</a>
              <a href="{{ path('aprobacionEditar', {'idConcurso': idConcurso}) }}" class="btn btn-success">Editar</a>
            </div>

          
          </div>
        </div>
      </div>
    </div>  


    <div class="container">
    <div class="alert alert-[[estadoMensaje]] alert-dismissible" id="MensajeCtrl" ng-show="mostrarMensaje === true" role="alert">[[textoMensaje]]<button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button></div>
    </div>

    <div class="section">
      <div class="container">
        <div class="row">
          <div class="col-md-12 table-responsive">
            <table class="table table-striped table-hover" ng-controller="gestionInfoCtrl">
              <thead>
                	<tr>
	                  	    
      
                    <th></th>     
	                  <th class="text-left">Aprobación</th>
                    <th></th>
	                  <th class="text-left">Fecha de Inicio</th>
                    <th></th>
                    <th class="text-left">Fecha de finalización</th>
	                                 
	            
	                </tr>
	           </thead>
	           <tbody>
               
                {% set consecutivo = 1 %}
	    	
	    		{% for aprobacion in aprobacion %}
	                
	      
	                  
                       
                    <th></th>              
	                  <td class="text-left">{{ aprobacion.aprobacion}}</td>
                    <th></th>
	                  <td class="text-left">{{ aprobacion.fechainicio |  date('d - m - Y')}}</td>
                    <th></th>
                    <td>{{ aprobacion.fechafinalizacion |  date('d - m - Y')}}</td>

	                </tr>
	                {% set consecutivo = consecutivo + 1 %}
	                {% else %}
	                
	                <tr>
	                  	<td colspan="9" class="info">No se encontraron registros.</td>
	                </tr>
	                
	                {% endfor %}
              
               </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>  

       
{% endblock %}
